"use client";

import type { ReactNode } from "react";

/** Placeholder — session comes from Supabase cookies; no global React context required. */
export function AuthSessionProvider({ children }: { children: ReactNode }) {
  return children;
}
