import { PlaceholderModule } from "@/components/dashboard/placeholder-module";

export default function JournalPage() {
  return (
    <PlaceholderModule
      title="Journal"
      description="Automatic trade stream, screenshots, and tags—persisted per account in the data milestone."
    />
  );
}
