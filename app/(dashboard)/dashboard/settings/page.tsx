import { PlaceholderModule } from "@/components/dashboard/placeholder-module";

export default function SettingsPage() {
  return (
    <PlaceholderModule
      title="Settings"
      description="Profile, notifications, API keys, and workspace preferences."
    />
  );
}
