import { PlaceholderModule } from "@/components/dashboard/placeholder-module";

export default function PlaybooksPage() {
  return (
    <PlaceholderModule
      title="Playbooks"
      description="If-then rules, pre-market checks, and adherence scoring against live executions."
    />
  );
}
