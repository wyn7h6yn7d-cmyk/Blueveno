import { PlaceholderPage } from "@/components/app/placeholder-page";

export default function PlaybooksPage() {
  return (
    <PlaceholderPage
      title="Playbooks"
      description="If-then rules and adherence tracking against your journal entries."
    />
  );
}
